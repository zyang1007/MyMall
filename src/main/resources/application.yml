server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mall_learning?useSSL=false&serverTimezone=UTC
    username: root
    password: rootpass

mybatis:
  mapper-locations:
    - classpath:mapper/*.xml
    - classpath*:com/**/mapper/*.xml